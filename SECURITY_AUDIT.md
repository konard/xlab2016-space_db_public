# Отчет по аудиту безопасности SpaceDb

**Дата:** 2025-11-03
**Аудитор:** Claude Code
**Репозиторий:** xlab2016/space_db_public

---

## Резюме

Проведен комплексный аудит безопасности репозитория SpaceDb. Выявлено **несколько критических и умеренных уязвимостей**, требующих немедленного внимания. Основные проблемы связаны с хранением учетных данных, конфигурацией безопасности и отсутствием защиты чувствительных данных.

---

## 🔴 Критические уязвимости

### 1. Хардкод-пароли в Docker Compose файле

**Расположение:** `src/docker-compose-db.yml:13`

```yaml
POSTGRES_PASSWORD: 'pguserpass'
```

**Проблема:**
- Пароль базы данных PostgreSQL захардкожен в файле docker-compose
- Этот файл находится в системе контроля версий (git)
- Любой с доступом к репозиторию может увидеть пароль

**Риск:** Высокий - несанкционированный доступ к базе данных

**Рекомендации:**
1. Удалить пароль из файла
2. Использовать переменные окружения или Docker secrets:
```yaml
POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
```
3. Создать `.env` файл (добавить в .gitignore):
```bash
POSTGRES_PASSWORD=secure_password_here
```

---

### 2. Примеры паролей в документации

**Расположение:**
- `README.md:199`
- `CLAUDE.md:88`

```bash
Password=pguserpass
```

**Проблема:**
- В документации показаны примеры с реальными паролями
- Если эти пароли используются в продакшене - критическая уязвимость

**Риск:** Высокий - если используются в продакшене

**Рекомендации:**
1. Проверить, не используются ли эти пароли в продакшене
2. Заменить примеры на плейсхолдеры:
```bash
Password=YOUR_PASSWORD_HERE
```

---

### 3. Пустые значения в конфигурации

**Расположение:** `src/SpaceDb/appsettings.json`

```json
"tokenManagement": {
  "secret": "",
  "issuer": "",
  "audience": ""
}
```

**Проблема:**
- JWT secret пустой - аутентификация не работает безопасно
- В Production это может привести к серьезным уязвимостям

**Риск:** Критический для Production

**Рекомендации:**
1. Никогда не коммитить реальные значения в Git
2. Использовать User Secrets для разработки:
```bash
dotnet user-secrets set "tokenManagement:secret" "your-secret-key-here"
```
3. В Production использовать переменные окружения или Azure Key Vault

---

## 🟡 Умеренные уязвимости

### 4. HTTPS отключен в продакшене

**Расположение:**
- `src/SpaceDb/Program.cs:95` - `RequireHttpsMetadata = false`
- `src/SpaceDb/Program.cs:126` - `// app.UseHttpsRedirection();` (закомментировано)

**Проблема:**
- HTTPS редирект закомментирован
- JWT не требует HTTPS метаданных
- Передача токенов по HTTP небезопасна

**Риск:** Средний - перехват токенов и данных

**Рекомендации:**
1. Раскомментировать `app.UseHttpsRedirection();`
2. Изменить `RequireHttpsMetadata = true` для Production
3. Использовать условную логику:
```csharp
_.RequireHttpsMetadata = !builder.Environment.IsDevelopment();
```

---

### 5. IdentityModel ShowPII включен

**Расположение:** `src/SpaceDb/Program.cs:87`

```csharp
IdentityModelEventSource.ShowPII = true;
```

**Проблема:**
- Персональные данные (PII) будут выводиться в логи
- Это может нарушать GDPR и другие нормы защиты данных

**Риск:** Средний - утечка персональных данных в логи

**Рекомендации:**
1. Включать только в Development:
```csharp
if (builder.Environment.IsDevelopment())
{
    IdentityModelEventSource.ShowPII = true;
}
```

---

### 6. Пустые connection strings

**Расположение:** `src/SpaceDb/appsettings.json:10`

```json
"PostgresConnection": "Server=;Port=5432;Database=spaces_db;User Id=;Password=;Pooling=false"
```

**Проблема:**
- Сервер, User Id и Password пустые
- Может привести к ошибкам конфигурации

**Риск:** Низкий - но может вызвать проблемы в продакшене

**Рекомендации:**
1. Использовать User Secrets для разработки
2. Использовать переменные окружения для продакшена

---

### 7. Elasticsearch логирование без аутентификации

**Расположение:** `src/SpaceDb/appsettings.json:71`

```json
"nodeUris": "http://localhost:9200"
```

**Проблема:**
- Elasticsearch URL без аутентификации
- Если Elasticsearch доступен извне - логи могут быть доступны всем

**Риск:** Средний - утечка логов приложения

**Рекомендации:**
1. Использовать HTTPS и аутентификацию
2. Ограничить доступ к Elasticsearch через firewall

---

### 8. Отсутствие Rate Limiting

**Проблема:**
- Нет защиты от brute-force атак на аутентификацию
- Нет ограничения на количество запросов к API

**Риск:** Средний - DoS атаки и brute-force

**Рекомендации:**
1. Добавить AspNetCoreRateLimit middleware
2. Настроить ограничения для endpoint'ов аутентификации

---

## 🟢 Низкие риски

### 9. Неполный .gitignore для чувствительных файлов

**Проблема:**
- .gitignore не включает все возможные файлы с секретами

**Рекомендации:**
Добавить следующие паттерны:
```gitignore
# Environment files
.env
.env.*
!.env.example

# User secrets
appsettings.*.local.json
secrets.json

# IDE-specific files with credentials
.idea/**/workspace.xml
.vs/

# Database files
*.db
*.db-shm
*.db-wal
rocksdb/

# SSL certificates
*.pfx
*.p12
*.pem
*.key
```

---

## ✅ Положительные моменты

1. **JWT аутентификация настроена** - используется стандартный механизм
2. **Authorize атрибуты применены** - все контроллеры защищены аутентификацией
3. **EF Core используется** - защита от SQL инъекций через параметризованные запросы
4. **Логирование настроено** - Serilog для мониторинга безопасности
5. **Нет явных секретов API токенов** - не найдены sk-, ghp- или glpat- токены

---

## Рекомендации по приоритетам

### Немедленно (24 часа)
1. ✅ Удалить пароль из docker-compose-db.yml
2. ✅ Обновить пароли, если они используются в продакшене
3. ✅ Добавить .env в .gitignore
4. ✅ Настроить JWT secret через User Secrets

### В течение недели
5. Включить HTTPS редирект
6. Отключить ShowPII для Production
7. Настроить User Secrets для разработки
8. Добавить rate limiting

### В течение месяца
9. Настроить Azure Key Vault или аналог для Production
10. Провести тестирование на проникновение
11. Настроить мониторинг безопасности
12. Добавить CORS политики

---

## Чек-лист для разработчиков

- [ ] Никогда не коммитить файлы .env
- [ ] Использовать User Secrets для локальной разработки
- [ ] Проверять .gitignore перед коммитом
- [ ] Использовать переменные окружения в Production
- [ ] Ротация секретов каждые 90 дней
- [ ] Включать HTTPS для Production
- [ ] Регулярно обновлять зависимости (dotnet list package --outdated)
- [ ] Проверять логи на наличие PII перед развертыванием

---

## Дополнительные инструменты для проверки

Рекомендуется использовать следующие инструменты:

1. **GitGuardian** или **TruffleHog** - сканирование секретов в Git истории
2. **OWASP Dependency-Check** - проверка уязвимостей в зависимостях
3. **Snyk** - сканирование .NET проектов
4. **SonarQube** - анализ качества и безопасности кода

---

## Заключение

Репозиторий содержит несколько критических проблем безопасности, главным образом связанных с хранением учетных данных. Большинство проблем могут быть исправлены в течение нескольких часов. **Рекомендуется немедленно удалить хардкод-пароли и настроить безопасное хранение секретов.**

После применения рекомендаций уровень безопасности значительно повысится.
